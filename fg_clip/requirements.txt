# 基础框架（与 blm_clip 共用环境）
torch>=2.2.0
transformers>=4.49.0
accelerate>=0.30.0
timm>=0.9.0

# 数据生产线
ultralytics>=8.3.0        # YOLO-World 开放词汇检测（Step 3 grounding）
spacy>=3.7.0              # 引用表达式抽取（Step 2）
# 中文模型需额外安装：python -m spacy download zh_core_web_sm

# 工具
Pillow>=10.0.0
numpy>=1.26.0
PyYAML>=6.0.0
tqdm>=4.66.0
requests>=2.31.0
